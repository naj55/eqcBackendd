version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install # Install dependencies
    build:
      commands:
        - npm run build # Adjust this command based on your backend
  artifacts:
    baseDirectory: . # Specify the directory for the backend
    files:
      - "**/*" # Include all files
  cache:
    paths:
      - node_modules/**/*
